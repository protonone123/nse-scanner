# NSE Scanner v3.7 Requirements
# ==============================

# Core dependencies
yfinance>=0.2.28
pandas>=1.3.0
numpy>=1.20.0
requests>=2.25.0
flask>=2.0.0
flask-cors>=3.0.10

# Database
sqlite3  # built-in with Python

# Optional but HIGHLY recommended for Gap 1/3/5 fixes:
# Gap 1: Official NSE OHLCV data (replaces yfinance for 1d bars)
nsepy>=1.0.3

# Gap 3: Institutional fundamentals (via Screener.in)
# (Uses requests, which is already listed above)

# Gap 5: NSE-specific data (delivery %, F&O OI, FII/DII, bulk deals)
# (Uses requests and yfinance, which are already listed above)

# Development (optional)
pytest>=6.0.0
pytest-cov>=2.12.0
black>=21.0.0
flake8>=3.9.0

# Telegram notifications (optional)
requests>=2.25.0
